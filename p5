#include <stdio.h> 

#include <unistd.h> 
int main() { 
    pid_t pid; 
    pid = fork(); 
    if(pid == 0) { 
        printf("Child Process\n"); 
        printf("Child ID = %d\n", getpid()); 
       printf("Parent ID = %d\n", getppid()); 
    } 

    else { 

        printf("Parent Process\n"); 

        printf("Parent ID = %d\n", getpid()); 

        printf("Child ID = %d\n", pid); 
    } 
    return 0; 
} 
